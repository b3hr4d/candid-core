interface:
  display_name: "Candid Core Issue Pipeline"
  short_description: "Prioritize and deliver Candid Core issues"
  default_prompt: "Use $candid-core-issue-pipeline to prioritize the backlog or safely handle a Candid Core issue through the authorized workflow."

// Shared value types for the Sovereign Wallet canister, v2 line.
type Tokens = record { e8s : nat64 };
type Memo = opt text;

type Account = record {
  owner : principal;
  subaccount : opt blob;
};
